using MediaPlayer;

// Top-level entry point for the .NET for iOS app.
// Use a different AppDelegate class by passing its type instead of typeof(AppDelegate).
UIApplication.Main(args, null, typeof(AppDelegate));
